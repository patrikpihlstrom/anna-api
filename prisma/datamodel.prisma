type User {
  id: ID! @id
  name: String!
  key: String
}

type Job {
  id: ID! @id
  container: String
  worker: String
  driver: String!
  site: String!
  status: String! @default(value: "pending")
  log: String
}
